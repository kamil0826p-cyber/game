# Implementation note

The eleven male warrior outfits are generated as renderer-ready 128x192 SVG data images. Each sheet contains four 32x48 animation frames in each of the four direction rows used by the existing renderer. The procedural source keeps the silhouettes and palettes independently editable while preserving PNG assets as a compatibility fallback.
